# Inko runtime overview

Inko is always the active assistant context inside Proof of Thesis. Deterministic capabilities extract, parse, index, calculate, and retrieve facts; a loaded model only interprets facts when reasoning or prose is needed.
