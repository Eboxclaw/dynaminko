# Tools

Tools are low-level deterministic operations declared in `src/lib/tools/registry.ts`. Tools declare inputs, output, access, and live status. Approval policy remains authoritative for write, edit, delete, execute, and external actions.
