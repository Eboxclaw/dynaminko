# Commands

Commands are semantic operations declared in `src/lib/commands/registry.ts`. Commands execute deterministically through bounded runners and return compact `CommandResult` payloads for UI cards and model observations.
